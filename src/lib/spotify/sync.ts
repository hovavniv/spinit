import 'server-only';

export async function syncTasteProfile(partnerId: string): Promise<void> {
  // Full implementation is task 8. This stub exists so the callback (task 7)
  // has something real to call; task 8 replaces the body.
}
